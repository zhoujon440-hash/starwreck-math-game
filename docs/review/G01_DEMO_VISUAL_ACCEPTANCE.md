# G01 Demo 视觉验收索引

## 生成方式

`tests-e2e/g01-demo.spec.ts` 在 1366×768 与 1920×1080 两个 Playwright 项目中
运行同一套真实浏览器流程。测试开启视频和 trace，截图写入 Playwright
`test-results` 输出目录。G01 Demo Gate 上传：

- `g01-demo-visual-acceptance-<run_number>`
- `starwreck-g01-demo-0.1.0-<run_number>`

## 连续八场证据

每个分辨率均包含八场的初始、中间、完成状态，以及最终 G02 只读交接：

1. SCN00：断电领航舱、找物/供电中间态、恢复完成态。
2. SCN01：七码离线、维修组件中间态、七码正常完成态。
3. SCN02：任务台初始态、任务链整理中间态、任务单完成态。
4. SCN03：漏气货舱初始态、应急物资中间态、复压完成态。
5. SCN04：破损星图初始态、碎片拼合中间态、坐标锁定完成态。
6. SCN05：垃圾雨航线初始态、旁路与路线中间态、安全落点完成态。
7. SCN06：远距观测舱初始态、信号 HOS / 三项授权中间态、求救源锁定完成态。
8. SCN07：近地轨道初始态、落点扫描/软失败/缓冲中间态、舱门打开完成态。
9. G02：旧屏幕谷外缘只读交接与刷新后任务证据记录。

PR-C 专项流程还提供 SCN07 安全节点、刷新后安全节点、继续恢复、三项基础能力、
任务与证据记录的成对证据。

## 自动断言

- 新游戏从 SCN00 连续操作到 SCN07，不调用存档注入、跳场景或测试菜单。
- SCN06 找物目标可以加载、拾取并从场景图层消失。
- 三项基础能力按搜寻、分析、寻路顺序授权；三项高级能力始终锁定。
- SCN07 的 S1—S4 软失败保存并恢复失败前正确进度，刷新保持安全节点。
- 关键物错误使用不消耗；正确操作更新场景、背包和存档。
- G01 完成后刷新仍在 G02 交接；G02 没有可操作玩法。
- `world_star_core_count=0`。
- 浏览器 console error 和 page error 为 0。
- 生产 PWA 使用项目级 base path，Service Worker 接管后离线刷新成功。

截图只用于 CI 验收材料，正式运行界面不显示热点框、状态编号或开发文字。
