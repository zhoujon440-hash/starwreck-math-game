# G01 PR-A 纠正视觉验收索引

`G01 PR-A Gate` 上传：

`g01-pr-a-corrective-visual-acceptance-<run_number>`

两种分辨率各有 45 张真实运行时截图：

- `test-results/g01-pr-a-visual/1366x768/`
- `test-results/g01-pr-a-visual/1920x1080/`

截图前 Playwright 会等待所有 `<img>`、CSS `background-image` 和字体完成加载
与解码；任一素材没有像素或加载失败都会使测试失败。这修复了旧 artifact
`8716445085` 中 1920×1080 HOS 区域偶发空白的问题。

## 基础流程与错误使用成对证据

- `01`—`05`：SCN-G01-02 初始、对白、线索、地图近景、任务依赖谜题。
- `06a` / `06b`：SCN-G01-02 错误使用前后，维修清单仍在背包。
- `06` / `07`：正确使用、任务链 S6。
- `07b`：正式 `HS-G01-0012` 未被复用，运行时货舱入口独立存在。
- `08`—`10`：SCN-G01-03 初始、完整 HOS、物品进入背包。
- `10a` / `10b`：SCN-G01-03 错误使用前后，密封胶带仍在背包。
- `11`—`15`：测压、补片、安全恢复、复压和完成。

## 持久化恢复、历史、档案与边界

- `13` / `13b` / `13c`：失败后安全节点、刷新仍在安全节点、继续恢复 S4。
- `16a` / `16b`：对话历史刷新前后。
- `17a` / `17b`：角色档案刷新前后。
- `18a` / `18b`：SCN-G01-03 S6 刷新前后。
- `19`：SCN-G01-04 仅为“星图缺口”边界，未提供运行时内容。

## S1—S4 软失败矩阵

每一阶段都提供四张成对/连续证据：

- `20-sN-failure-before.png`：失败前真实进度。
- `21-sN-actual-safe-node.png`：进入
  `SCN-G01-03:cargo-safety-door`。
- `22-sN-safe-node-after-refresh.png`：刷新后仍在安全节点。
- `23-sN-resumed-original-progress.png`：继续后恢复失败前 S 状态。

矩阵同时断言物品、HOS、谜题、证据、正确热点、对白历史和档案发现不丢失；
未取得的压力证据在 S1—S3 不会出现；已取得的 S4 压力证据会保留；物品不会
重复发放。两种分辨率浏览器控制台错误均必须为 0。
